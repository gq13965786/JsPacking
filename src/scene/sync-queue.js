Object.assign(ape, function () {
  var SyncQueue = function () {};

  SyncQueue.prototype.runSync = function () {};
  SyncQueue.prototype.erase = function () {};
  var bs = function (index, s, e, k) {};
  SyncQueue.prototype.push = function (p, v) {};

  return {
    SyncQueue: SyncQueue
  };
}());
