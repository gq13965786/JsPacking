describe('ape.AssetReference', function () {
  var app;
  var parent;
  var load;
  var remove;
  var add;

  beforeEach(function () {

  });
  afterEach(function () {

  });

  it('ape.AssetReference, load callback', function () {});
  it('ape.AssetReference, add callback', function () {});
  it('ape.AssetReference, remove callback', function () {});
});
