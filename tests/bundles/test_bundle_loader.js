describe('Test Bundle Loader', function () {
  beforeEach(function () {});

  afterEach(function () {});

  it('should load bundle asset', function () {});
  it('should load bundle asset without using web workers', function () {});
  it('should load assets from bundle', function () {});
  it('should load assets from bundle without using web workers', function () {});
  it('should fail loading assets if the bundle has not started loading', function () {});
})
